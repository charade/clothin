import { makeStyles } from "@material-ui/core";

export const useLandingStyles = makeStyles(theme => ({
    root : {
        position : 'absolute',
        width : '100%',
        height : '100%',
        overflow : 'hidden',
        zIndex : 5
    }
}))