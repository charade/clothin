import { makeStyles } from "@material-ui/core";

export const useNavigatoinOptionsStyle = makeStyles(theme => ({
    list : {
        display : 'flex',
        listStyle : 'none'
    },
    item : {
        
    }
}))